# Dépendances du pipeline de mesure DNS
# Minimal — pas de LaTeX, Jupyter, Office, OCR

# HTTP et DNS
requests>=2.31.0
dnspython>=2.4.0
python-dotenv>=1.0.0

# Analyse et stockage
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
pyarrow>=14.0.0

# Figures
matplotlib>=3.8.0
